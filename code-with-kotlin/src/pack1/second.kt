package pack1
fun main() {
//Arrays:collection of elements with same type reference and same name
        var names = arrayOf("gunamani", "sakuntala", "shubhashish", "subhashree")
        println(names[0])
        println(names[3])
        println(names.size)

        var name2:String = "Sita"
        println(name2)
        var id = "123456"
        println(id)
        println(name2.length)
        println(name2.toUpperCase())
        println(name2.toLowerCase())
}